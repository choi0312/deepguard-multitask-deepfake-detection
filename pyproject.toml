[project]
name = "deepguard-multitask-deepfake-detection"
version = "0.1.0"
description = "Multi-task deepfake detection and manipulation-method classification pipeline."
readme = "README.md"
requires-python = ">=3.10"

[tool.black]
line-length = 100

[tool.isort]
profile = "black"
